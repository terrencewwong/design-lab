// @flow
import {
  flex,
  horizontalCenter,
  fullWidth,
  horizontalPadding,
  maxWidth
} from './webpage-layout-rules'

export const wrapperStyles = [
  flex,
  horizontalCenter
].join('')

export const contentStyles = [
  fullWidth,
  horizontalPadding,
  maxWidth
].join('')

